# AtlasForge AI visual assets

`atlasforge-ai-pipeline.png` is the user-supplied target-architecture diagram used in the project README. It is documentation, not a generated runtime artifact.

The implementation-status table in the root README is the source of truth for which boxes are production code today and which remain roadmap items.
