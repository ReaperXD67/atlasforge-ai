extends: atomy-us-openrouter.yaml

channel:
  name: "Atomy USA — Fast Preview"

script:
  target_minutes: 2
  min_words: 240
  max_words: 360

storyboard:
  target_scene_seconds: 8
  min_scene_seconds: 5
  max_scene_seconds: 11
  max_scenes: 20

runtime:
  keep_intermediates: true
